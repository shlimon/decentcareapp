import ConcernForm from '@pages/forms/complaints-forms/ConcernForm';
import React from 'react';

const ConcernFormPage = () => {
   return (
      <div>
         <ConcernForm />
      </div>
   );
};

export default React.memo(ConcernFormPage);
