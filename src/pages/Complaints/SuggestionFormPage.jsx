import SuggestionForm from '@pages/forms/complaints-forms/SuggestionForm';
import React from 'react';

const SuggestionFormPage = () => {
   return (
      <div>
         <SuggestionForm />
      </div>
   );
};

export default React.memo(SuggestionFormPage);
