import ComplementForm from '@pages/forms/complaints-forms/ComplementForm';
import React from 'react';

const ComplementFormPage = () => {
   return (
      <div>
         <ComplementForm />
      </div>
   );
};

export default React.memo(ComplementFormPage);
